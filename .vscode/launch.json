{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "확장 시작",
            "type": "extensionHost",
            "request": "launch",
            "runtimeExecutable": "${execPath}",
            "args": [
                "--extensionDevelopmentPath=${workspaceRoot}"
            ],
            "stopOnEntry": false,
            "sourceMaps": true,
            "outDirs": [
                "${workspaceRoot}/out/**/*.js"
            ],
            "preLaunchTask": "npm"
        }
    ]
}